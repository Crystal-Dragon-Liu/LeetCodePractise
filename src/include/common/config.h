#ifndef CONFIG_H
#define CONFIG_H
#include <vector>
#include <string>
#define INVALID_VALUE -1

#define STRIP_BAR "=============================="

using func_list = std::vector<std::string>;

using input_list_int = std::vector<std::vector<int> >;

#endif